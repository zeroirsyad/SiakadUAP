<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SiakadUAP"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".ui.pembayaran.PembayaranActivity"
            android:exported="false" />
        <activity
            android:name=".ui.jadwal.JadwalActivity"
            android:exported="false" />
        <activity
            android:name=".ui.info.InfoActivity"
            android:exported="false" />
        <activity
            android:name=".ui.absensi.AbsensiActivity"
            android:exported="false" />
        <activity
            android:name=".ui.absensi.Absensi"
            android:exported="false" />
        <activity
            android:name=".ui.khs.KhsActivity"
            android:exported="false" /> <!-- Splash Screen Activity (Satu-satunya LAUNCHER) -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SiakadUAP.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Login Activity -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" /> <!-- Main Activity -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false" /> <!-- KRS Activity -->
        <activity
            android:name=".ui.krs.KrsActivity"
            android:exported="false" />
    </application>

</manifest>